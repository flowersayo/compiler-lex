int add(int a,int b);

int main()
{


// 괄호짝이 안맞는 경우

    int j = 3; 
    const int MAX=5;

    while(j <= MAX {
		
	
    }


    while(j <= MAX) 
    }
	
    // 자료형이 일치하지 않는 경우 
    int i = 9.0; 
    float f = 3; 
   
    int arr[-3]; // 배열 크기가 음수인 경우

    // 선언되지 않은 식별자 사용
    c = 4; 

    // 함수 정의와 일치하지 않는 함수 호출
    add(3); // 매개변수 개수가 일치 x
    add(3.0,3.8); // 매개 변수 타입이 일치 x
    add(); // 매개변수 없이 호출

    return

    
}

